# Naming — DECIDED: Sous

**Winner: Sous** — like a sous-chef. The AI that preps everything so the user
(Head Chef) just plates and signs.

- App: `Sous` (`NEXT_PUBLIC_APP_NAME=Sous`)
- Tagline: "Your sous-chef for Cookie Chain" + "Yes, Chef!"
- Mark: chef's hat over a bitten cookie (`public/brand/sous-logo.svg`, favicon mirrors it)
- Loader lines: Preheating oven… / Tasting the sauce… / Plating…
- Package: `sous`

TODO before submission: check `sous.cook` availability via `resolve_domain` +
X handle + domain, then lock it in.
